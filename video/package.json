{
  "name": "lockr-vsl",
  "private": true,
  "description": "Remotion source for the Lockr motion-graphic VSL. Separate from the Next app so it never bloats the Vercel deploy. Render: npm run render -> out/lockr-vsl.mp4, then drop it in ../public.",
  "scripts": {
    "studio": "remotion studio src/index.ts",
    "still": "remotion still src/index.ts LockrVSL out/frame.png",
    "render": "remotion render src/index.ts LockrVSL out/lockr-vsl.mp4 --codec=h264"
  },
  "dependencies": {
    "@remotion/cli": "^4.0.0",
    "remotion": "^4.0.0",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  }
}
